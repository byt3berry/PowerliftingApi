[package]
name = "frontend"
edition = "2024"
version.workspace = true

[dependencies]
anyhow = "1.0.98"
maud = { version = "*", features = ["actix-web"] }
strum = "0.27.2"
types = { path = "../types" }
