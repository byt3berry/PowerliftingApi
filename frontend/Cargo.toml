[package]
name = "frontend"
version = "0.1.0"
edition = "2024"

[dependencies]
anyhow = "1.0.98"
maud = { version = "*", features = ["actix-web"] }
strum = "0.27.2"
types = { version = "0.1.0", path = "../types" }
